"use strict";

/**
 * The function returns the nth value of
 * the Pell numbers sequence
 *
 * @param {number} n (n >= 0)
 * @returns {number}
 */

function pellNumbers(number) {
    /*
     * Your task is to calculate the nth value of the
     * Pell numbers sequence.
     * https://en.wikipedia.org/wiki/Pell_number
     * Store the value in the pellNum variable.
     * Also take into consideration the documentation of the function!
     */
    // PLACE YOUR CODE BETWEEN THIS...

    // ...AND THIS COMMENT LINE!
    return pellNum;
}
module.exports = pellNumbers;