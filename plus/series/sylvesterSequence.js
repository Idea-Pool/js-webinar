"use strict";

/**
 * The function returns the nth value of
 * the Sylvester's sequence
 *
 * @param {number} n (n >= 0)
 * @returns {number}
 */

function sylvesterSequence(number) {
    /*
     * Your task is to calculate the nth value of the
     * Sylvester's sequence.
     * https://en.wikipedia.org/wiki/Sylvester%27s_sequence
     * Store the value in the sylvesterNum variable.
     * Also take into consideration the documentation of the function!
     * Since numbers can be very big, use %10^9 + 7.
     */
    // PLACE YOUR CODE BETWEEN THIS...

    // ...AND THIS COMMENT LINE!
    return sylvesterNum;
}
module.exports = sylvesterSequence;