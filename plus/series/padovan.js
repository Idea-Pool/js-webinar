"use strict";

/**
 * The function returns the nth value of
 * the Padovan sequence
 *
 * @param {number} n (n >= 0)
 * @returns {number}
 */

function padovanSequence(number) {
    /*
     * Your task is to calculate the nth value of the
     * Padovan sequence.
     * https://en.wikipedia.org/wiki/Padovan_sequence
     * Store the value in the padovanNum variable.
     * Also take into consideration the documentation of the function!
     */
    // PLACE YOUR CODE BETWEEN THIS...
   
    // ...AND THIS COMMENT LINE!
    return padovanNum;
}
module.exports = padovanSequence;