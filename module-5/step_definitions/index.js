const { Given, When, Then } = require("cucumber");

Given('the EPAM Careers page is opened', function () {
    // test code
    return 'pending';
});

Then('the EPAM Careers page should be opened', function () {
    // test code
    return 'pending';
});

Then(/the Search form should be (visible|hidden)/, function (visibility) {
    // test code
    return 'pending';
});

When('the Location filter box is clicked', function () {
    // test code
    return 'pending';
});

When(/the Country "(.+)" is selected/, function (country) {
    // test code
    return 'pending';
});

When(/the City "(.+)" is selected/, function (city) {
    // test code
    return 'pending';
});

Then(/the selected Location should be "(.+)"/, function (city) {
    // test code
    return 'pending';
});

When('the Department filter box is clicked', function () {
    // test code
    return 'pending';
});

When(/the Department "(.+)" is selected/, function (department) {
    // test code
    return 'pending';
});

Then(/the selected Department should be "(.+)"/, function (department) {
    // test code
    return 'pending';
});

When('the Search button is clicked', function () {
    // test code
    return 'pending';
});

Then(/the PositionName "(.+)" is visible/, function (positionName) {
    // test code
    return 'pending';
});

Then(/the Department of the position should be "(.+)"/, function (department) {
    // test code
    return 'pending';
});

Then(/the Location of the position should be "(.+), (.+)"/, function (city, country) {
    // test code
    return 'pending';
});

Then('an Apply button is visible', function () {
    // test code
    return 'pending';
});

Then('an Apply button is enabled', function () {
    // test code
    return 'pending';
});

When('an Apply button is clicked', function () {
    // test code
    return 'pending';
});

Then(/the Job description contains "(.+)"/, function (cityOrPosition) {
    // test code
    return 'pending';
});